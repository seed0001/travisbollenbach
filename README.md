# travisbollenbach.com

Temporary holding page while the site is rebuilt.

The live app currently contains:

- the animated construction page at `/`
- the message portal API at `/api/comments`

Everything else has been cleared out so the next application can be built back
up step by step.
